import React from 'react';

const AddBookBtn = () => {
  const onAddClick = () => {
    
  };
  return (
    <div className="open-search">
      <button>Add a book</button>
    </div>
  );
};

export default AddBookBtn;
